<?php declare(strict_types=1);

namespace BabDev\WebSocket\Server;

/**
 * The message component interface defines a middleware component for a controller on the WebSocket server.
 */
interface MessageComponentInterface
{
}
